{
  "title": "MeshAnnotation",
  "type": "object",
  "description": "Specify how the mesh should be interpreted by the camera",
  "allOf": [ { "$ref": "glTFProperty.schema.json" } ],
  "properties": {
    "node": {
      "allOf": [{ "$ref": "glTFid.schema.json" }],
      "description": "The index of the node that attached to target mesh."
    },
    "type": {
      "title": "FirstPersonType",
      "type": "string",
      "enum": ["auto", "both", "thirdPersonOnly", "firstPersonOnly"],
      "description": "How the camera interprets the mesh."
    },
    "extensions": { },
    "extras": { }
  },
  "required": [
    "node",
    "type"
  ]
}
